{% extends 'base.html.twig' %}

{% block title %}Portfolio{% endblock %}

{% block body %}

    <section id="book-container">
        <div class="pictures col-md-9 mx-auto m-5 d-flex justify-content-center flex-wrap">
            {% for picture in portfolio %}

                <div class="picture m-4"
                     style="background: url('{{ asset('uploads/portfolio/') }}{{ picture.picture }}'); width: 200px; height: 200px; background-size: cover; background-position: center;">

                </div>

            {% endfor %}
            {% for picture in portfolio %}

                <div class="picture m-4"
                     style="background: url('{{ asset('uploads/portfolio/') }}{{ picture.picture }}'); width: 200px; height: 200px; background-size: cover; background-position: center;">

                </div>

            {% endfor %}
        </div>
    </section>

{% endblock %}