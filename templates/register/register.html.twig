{% extends 'base.html.twig' %}

{% block title %}S'inscrire{% endblock %}

{% block body %}

    {{ form_start(form) }}

    <div class="col-md-10 mx-auto d-flex justify-content-center mt-5">

        <div class="container-register-left col-4 text-light">
            <div>{{ form_row(form.firstname) }}</div>
            <div>{{ form_row(form.lastname) }}</div>
            <div>{{ form_row(form.phone) }}</div>
        </div>
        <div class="col-1"></div>
        <div class="container-register-right col-4 text-light">
            <div>{{ form_row(form.email) }}</div>
            <div>{{ form_row(form.password) }}</div>
        </div>

    </div>
    <div class="d-flex justify-content-center my-5">
        {{ form_row(form.submit) }}
    </div>

    {{ form_end(form) }}

{% endblock %}